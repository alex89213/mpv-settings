# mpv writes these at runtime; they are state, not configuration.
portable_config/cache/
portable_config/watch_later/
portable_config/script-opts/*.conf.bak

# log-file target in mpv.conf
mpv-log.txt

Screenshots/
